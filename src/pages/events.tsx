import useSWR from 'swr'; export default function Events(){ const { data } = useSWR('/api/events/list', r=>fetch(r).then(d=>d.json())); return (<div className='container'><div className='card'><h1>Events</h1><div className='grid grid-3'>{(data?.events||[]).map((e:any)=>(<a key={e.slug} className='card' href={`/event/${e.slug}`}><h2>{e.name}</h2><div className='small'>{new Date(e.dateISO).toLocaleString()}</div><div><span className='badge'>{e.slug}</span></div></a>))}</div></div></div>) }
