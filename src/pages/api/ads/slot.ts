import type { NextApiRequest, NextApiResponse } from 'next'; import { setAdSlot } from '@/lib/db'; export default function handler(req:NextApiRequest,res:NextApiResponse){ const { slug,id,url,kind,dur } = req.body||{}; if(!slug||!id) return res.status(400).json({error:'missing'}); const slots=setAdSlot(String(slug),{id,url,kind,dur}); if(!slots) return res.status(404).json({error:'event/slot'}); res.json({ slots }); }
